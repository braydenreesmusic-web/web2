# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google Gemini AI Configuration (Free tier: 60 requests/minute)
# Get your key at: https://makersuite.google.com/app/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key

# Optional: Firebase Configuration (alternative)
# VITE_FIREBASE_API_KEY=your_firebase_api_key
# VITE_FIREBASE_AUTH_DOMAIN=your_firebase_auth_domain
# VITE_FIREBASE_PROJECT_ID=your_firebase_project_id

# Web Push / Notifications
# Generate VAPID keys with: `npx web-push generate-vapid-keys`
# Put the `publicKey` here for the frontend, and `VAPID_PUBLIC`/`VAPID_PRIVATE` for servers.
VITE_VAPID_PUBLIC=your_vapid_public_key

# Server / CI secrets (do NOT commit real values):
# SUPABASE_URL=https://yourproject.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=eyJ... (service_role key)
# VAPID_PUBLIC=BLah...
# VAPID_PRIVATE=abc...

# SendGrid (server-side only)
# Create an API key in SendGrid and paste it here. THIS MUST NEVER be prefixed with VITE_ (those vars are exposed to the browser).
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=you@yourdomain.com
